# מימוש שמירה אוטומטית 💾

## סקירה כללית

מערכת השמירה האוטומטית מבטיחה שכל השינויים שמבצע המשתמש באתר נשמרים אוטומטית ונשמרים בין הפעלות.

## איך זה עובד?

### 🔄 שתי שכבות שמירה

1. **עם Supabase מוגדר** - כל השינויים נשמרים ישירות למסד הנתונים בענן
2. **בלי Supabase** - כל השינויים נשמרים ב-localStorage של הדפדפן

### 🎯 מה נשמר אוטומטית?

- ✅ **משימות (Tasks)** - יצירה, עריכה, מחיקה
- ✅ **מיילסטונים (Milestones)** - הוספה, עדכון סטטוס, מחיקה
- ✅ **קטגוריות (Groups)** - יצירה, עריכה, מחיקה
- ✅ **אנשי קשר (People)** - הוספה, עריכה, מחיקה
- ✅ **עוקבים (Task Watchers)** - הוספה והסרה

## קבצים שנוצרו/שונו

### קבצים חדשים:
1. **`src/lib/localStorage.ts`** - שכבת ההתמדה המקומית
   - פונקציות לשמירה וטעינה של כל סוגי הנתונים
   - המרה אוטומטית של תאריכים
   - טיפול בשגיאות

2. **`src/components/AutoSaveIndicator.tsx`** - אינדיקטור ויזואלי
   - מראה "שומר..." במהלך שמירה
   - מראה "נשמר ✓" אחרי שמירה מוצלחת
   - מציג הודעה "שמירה אוטומטית מופעלת"

3. **`src/hooks/useAutoSaveStatus.ts`** - Hook למעקב אחר סטטוס השמירה
   - עוקב אחר מוטציות פעילות
   - מעדכן זמן שמירה אחרון

### קבצים שעודכנו:
1. **`src/lib/dataService.ts`** - לוגיקת הנתונים
   - כל הפונקציות עודכנו לתמוך ב-localStorage
   - פעולות CRUD מלאות עבור כל סוגי הנתונים

2. **`src/hooks/useData.ts`** - React Query hooks
   - נוספו hooks לניהול קטגוריות (useCreateGroup, useUpdateGroup, useDeleteGroup)

3. **`src/App.new-supabase.tsx`** - האפליקציה הראשית
   - נוסף AutoSaveIndicator לכותרת
   - מעקב אחר סטטוס שמירה

4. **`src/components/tasks/TasksView.tsx`** - תצוגת משימות
   - נוסף כפתור "משימה חדשה"
   - נוסף Drawer ליצירת משימות חדשות

5. **`src/components/tasks/TaskDrawer.tsx`** - עריכת משימה
   - נוסף כפתור מחיקת משימה
   - שמירה אוטומטית של כל שינוי

6. **`src/main.tsx`** - נקודת הכניסה
   - עודכן להשתמש ב-App.new-supabase.tsx שתומך בשמירה אוטומטית

## איך להשתמש

### למשתמש הקצה:
1. **ערוך משימה** - לחץ על משימה כלשהי
2. **שנה פרטים** - ערוך שם, תיאור, אחראי
3. **לחץ "שמור שינויים"** - השינויים נשמרים אוטומטית
4. **הוסף מיילסטונים** - השמירה אוטומטית
5. **צור משימה חדשה** - לחץ על "משימה חדשה" בכותרת
6. **רענן את הדף** - כל השינויים יישארו!

### אינדיקטור השמירה
בכותרת העליונה של האתר תראה:
- 🔄 **"שומר..."** - כשהנתונים נשמרים
- ✅ **"נשמר"** - מיד אחרי שמירה מוצלחת
- ☁️ **"שמירה אוטומטית מופעלת"** - במצב רגיל

## טכנולוגיות

- **React Query** - ניהול מצב ו-cache
- **localStorage API** - שמירה מקומית
- **Supabase** (אופציונלי) - מסד נתונים בענן
- **Optimistic Updates** - עדכונים מיידיים בממשק

## יתרונות

1. ✅ **אין אובדן נתונים** - כל שינוי נשמר מיד
2. ✅ **עובד offline** - גם בלי אינטרנט (עם localStorage)
3. ✅ **מהיר ורספונסיבי** - עדכונים אופטימיים
4. ✅ **משוב ויזואלי** - המשתמש רואה מתי השמירה מתבצעת
5. ✅ **התמדה בין הפעלות** - השינויים נשארים גם אחרי רענון

## בדיקה

כדי לבדוק שהשמירה עובדת:
1. הרץ `npm run dev`
2. פתח את האתר בדפדפן
3. ערוך משימה או צור משימה חדשה
4. שים לב לאינדיקטור "שומר..." ואז "נשמר"
5. רענן את הדף (F5)
6. ודא שהשינויים נשארו

## פתיחת Developer Tools
בדפדפן, פתח את ה-Console ותוכל לראות את הנתונים ב-localStorage:
```javascript
// הצג את כל הנתונים השמורים
console.log('Tasks:', localStorage.getItem('grow.tasks'));
console.log('Milestones:', localStorage.getItem('grow.milestones'));
console.log('Groups:', localStorage.getItem('grow.groups'));
```

## מעבר ל-Supabase בעתיד

כשתרצה לעבור למסד נתונים בענן:
1. הגדר את משתני הסביבה ב-`.env`:
   ```
   VITE_SUPABASE_URL=your-project-url
   VITE_SUPABASE_ANON_KEY=your-anon-key
   ```
2. הקוד יעבור אוטומטית לשמור ב-Supabase במקום localStorage
3. הנתונים מ-localStorage לא יימחקו, אבל לא יעודכנו יותר

## תחזוקה

- **לנקות נתונים מקומיים**: פתח Console והרץ `localStorage.clear()`
- **לייצא נתונים**: העתק את הערכים מ-localStorage
- **לייבא נתונים**: הדבק ל-localStorage באמצעות Console

---

**הערה חשובה**: מכיוון ש-localStorage מוגבל לדפדפן ספציפי, אם המשתמש יעבור למחשב אחר או לדפדפן אחר, הוא לא יראה את הנתונים. לשיתוף בין מכשירים, יש להגדיר Supabase.
