# מערכת GROW - ניהול פרויקט לשיפור תהליכים בבית חולים

## מבנה המערכת המעודכן

המערכת תוכננה במיוחד לניהול פרויקטים לשיפור תהליכים בבית החולים, עם דגש על אפיון משימות מעמיק וניהול איכותי.

---

## תצוגות במערכת

### 📊 דשבורד - סיכום תהליכי שיפור
מציג מדדים רלוונטיים לשיפור תהליכים:
- **משימות פעילות** - סה"כ משימות בפרויקט
- **משימות עם KPI מוגדר** - כמה משימות יש להן מדד הצלחה ברור
- **מעבר לתאריך יעד** - משימות שדורשות תשומת לב מיידית
- **משימות עם חסמים** - משימות שדווחו עליהן בעיות או סיכונים

**סקשנים נוספים:**
- התקדמות לפי קטגוריה (כולל מספר משימות עם KPI וחסמים)
- משימות קריטיות (מעבר לתאריך יעד, ללא אחראי, עם חסמים)
- אחראים בולטים

### 📋 משימות - תצוגת שורות עם expand
תצוגה מרכזית לניהול יומיומי:

**שורה קומפקטית** (תצוגה מהירה):
- אייקון פתיחה/סגירה (חץ)
- קטגוריה (badge צבעוני)
- כותרת המשימה
- אחראי
- בר התקדמות + אחוזים
- ספירת מיילסטונים
- כפתור "ערוך" מהיר

**פתיחת שורה** (expand) - מציגה:
- תיאור מלא
- **אפיון תהליך**: מחלקה, שם תהליך, הגדרת הבעיה, מטרה
- **מדד הצלחה (KPI)**: שם מדד, Baseline, Target, תדירות מדידה
- **תאריכים ובעלי עניין**: תאריך התחלה, תאריך יעד, רשימת stakeholders
- **סיכונים ותלויות**: סיכונים וחסמים, תלויות במשימות אחרות, קישורים למסמכים
- רשימת מיילסטונים מלאה (עם סימון ✅/⭕)

**פיצ'רים:**
- חיפוש בכותרת, תיאור, אחראי
- פילטרים: לפי קטגוריה, לפי אחראי (מרובה)
- סידור לפי קטגוריות עם כותרות sticky
- Responsive למובייל (מסתיר עמודות משניות במסכים קטנים)

### 🌳 דיאגרמה - מבנה היררכי
דיאגרמת עץ ויזואלית:
- פרויקט (שורש) → קטגוריות → משימות
- קווים צבעוניים מחברים בין רמות
- לחיצה על משימה פותחת Drawer לעריכה
- **Hover על משימה** מציג tooltip עשיר:
  - כותרת ותיאור
  - מחלקה, תאריך יעד, KPI
  - בעלי עניין

---

## אפיון משימה - השדות החדשים

כל משימה כוללת:

### בסיסי
- **כותרת** (חובה)
- **קטגוריה** (חובה)
- **תיאור**
- **אחראי**

### אפיון תהליך
- **מחלקה** - המחלקה או היחידה שבה התהליך מתבצע
- **שם תהליך** - שם התהליך הספציפי
- **הגדרת הבעיה** - מה נקודת הכאב/הבעיה הנוכחית?
- **מטרה** - מה היעד המדיד שרוצים להשיג?

### מדידה (KPI)
- **שם מדד** - איזה KPI נמדד?
- **Baseline** - נקודת ההתחלה (מצב נוכחי)
- **Target** - היעד (מצב רצוי)
- **תדירות מדידה** - כמה פעמים נמדד? (שבועי/חודשי/וכו')

### ניהול
- **תאריך התחלה** - מתי המשימה מתחילה
- **תאריך יעד** - Deadline למשימה
- **בעלי עניין** - רשימת אנשים/גורמים מעורבים (מופרד בפסיקים)

### סיכונים ותלויות
- **סיכונים וחסמים** - מה עלול לעכב? מה הבעיות?
- **תלויות** - תלויות במשימות אחרות
- **קישורים ומסמכים** - לינקים לדרייב, מסמכים, וכו'

### מיילסטונים
- רשימת מיילסטונים (sub-tasks)
- כל מיילסטון ניתן לסימון ✅
- ההתקדמות מחושבת אוטומטית לפי מיילסטונים שהושלמו

---

## זרימת עבודה מומלצת

### 1. יצירת משימה
- לחץ "➕ משימה חדשה"
- מלא שדות בסיסיים (כותרת, קטגוריה, אחראי)
- פתח "פרטים מתקדמים" למילוי אפיון מלא
- הוסף מיילסטונים

### 2. עבודה יומיומית
- עבור לתצוגת "משימות"
- השתמש בחיפוש ופילטרים למציאת משימות רלוונטיות
- לחץ על שורה לפתיחה (expand) וקריאת פרטים
- לחץ "ערוך" לעריכה מלאה ב-Drawer

### 3. מעקב
- עבור לדשבורד לסיכום כולל
- שים לב למשימות קריטיות (מעבר לתאריך יעד, ללא אחראי, עם חסמים)
- בדוק התקדמות לפי קטגוריה

### 4. דיווח
- השתמש בדיאגרמת העץ לתצוגה ויזואלית
- ייצא נתונים ל-JSON לצורך גיבוי או שיתוף

---

## שינויים מהגרסה הקודמת

### הוסר
- ❌ לוח קנבן (Kanban board)
- ❌ Drag & Drop בין עמודות סטטוס
- ❌ סימונים "בתהליך/הושלם/לא התחיל" (שהתבססו רק על progress)
- ❌ פילטר התקדמות

### הוסף
- ✅ תצוגת שורות עם expand/collapse
- ✅ 11 שדות חדשים לאפיון תהליכי שיפור
- ✅ דשבורד ממוקד בשיפור תהליכים
- ✅ Tooltips עשירים בדיאגרמת העץ
- ✅ תמיכת מובייל משופרת
- ✅ טיפוגרפיה משופרת וריווחים נקיים

---

## טכנולוגיות

- **React 18** + TypeScript
- **Vite** - Build tool
- **Tailwind CSS** - Styling
- **RTL Support** - תמיכה מלאה בעברית
- **Responsive Design** - מותאם לדסקטופ, טאבלט, מובייל
- **CSS Animations** - אנימציות חלקות
- **Toast Notifications** - התראות מונפשות

---

## הרצה מקומית

```bash
# התקנת תלויות
npm install

# הרצת שרת פיתוח
npm run dev

# בניית production
npm run build

# תצוגה מקדימה של build
npm run preview
```

---

## קבצי מפתח

- `src/App.tsx` - קומפוננטה ראשית עם כל הלוגיקה
- `src/components/Toast.tsx` - מערכת התראות
- `src/types/index.ts` - הגדרות TypeScript
- `src/lib/mockData.ts` - נתוני דוגמה
- `supabase/schema.sql` - סכמת DB (לעתיד)
- `supabase/seed.sql` - נתוני seed (לעתיד)

---

## העלאה לשרת

```bash
# בניה
npm run build

# קבצי ה-dist/ מוכנים להעלאה
# העלה את כל תוכן dist/ לשרת שלך (public_html/)
```

ראה `DEPLOYMENT.md` להוראות מפורטות.

---

## שלבים הבאים (אופציונלי)

1. **חיבור ל-Supabase** - לשמירת נתונים במסד נתונים אמיתי
2. **Authentication** - הזדהות משתמשים
3. **Excel Export** - ייצוא למסמכי אקסל
4. **Realtime Sync** - סנכרון בזמן אמת בין משתמשים
5. **File Attachments** - צרופות קבצים למשימות
6. **Comments** - הערות ודיונים על משימות
7. **Activity Log** - לוג פעילויות

---

## תמיכה

המערכת מוכנה לשימוש מלא במצב mock data.

לשאלות: בדוק את `USER_GUIDE.md` למדריך מפורט.
