# מפת העץ - תכונות Zoom חדשות

## מה נוסף

הוספנו zoom controls למפת העץ כדי לאפשר תצוגה מלאה במובייל.

## תכונות

### 1. Zoom אוטומטי לפי גודל מסך
- **מובייל (< 768px):** התחלה ב-50% zoom - רואים את כל העץ
- **דסקטופ (>= 768px):** התחלה ב-100% zoom - גודל רגיל

### 2. כפתורי Zoom (מותאמים למגע)
בפינה השמאלית התחתונה:

- **+ (ירוק)** - הגדלה (עד 200%)
- **− (אדום)** - הקטנה (עד 30%)
- **⊡ (כחול)** - התאם למסך (חזרה למצב ברירת המחדל)
- **אינדיקטור** - מציג את אחוז ה-zoom הנוכחי

גודל כפתורים: 48x48px (ידידותי למגע)

### 3. קיצורי מקלדת

כשאתה במפת העץ:
- `+` או `=` - הגדלה
- `-` - הקטנה
- `0` - איפוס למצב ברירת מחדל

### 4. הודעת עזרה
בפעם הראשונה שפותחים את מפת העץ, מופיעה הודעת עזרה שמסבירה את תכונת ה-zoom.
ההודעה נשמרת ב-localStorage ולא תופיע שוב.

## איך זה עובד

1. **במובייל:** פתח את מפת העץ → רואה את כל המבנה מוקטן → לחץ + להגדלה של אזור מסוים
2. **בדסקטופ:** רואה את העץ בגודל מלא → אפשר להקטין אם יש הרבה משימות
3. **למידה מהירה:** הודעת העזרה מופיעה רק בפעם הראשונה

## שינויים טכניים

### קובץ: src/App.tsx

1. **State חדש:**
   - `treeZoom` - שומר את רמת ה-zoom (0.3-2.0)
   - `showTreeZoomHint` - מציג הודעת עזרה בפעם הראשונה

2. **פונקציות:**
   - `zoomIn()` - מגדיל ב-20% (עד 200%)
   - `zoomOut()` - מקטין ב-20% (עד 30%)
   - `resetZoom()` - מחזיר ל-50% במובייל או 100% בדסקטופ

3. **Transform Container:**
   - העץ כולו עטוף ב-div עם `transform: scale(treeZoom)`
   - `transformOrigin: 'top center'` - ה-zoom מתחיל מלמעלה
   - מעבר חלק עם `transition: 'transform 0.3s ease'`

4. **Responsive:**
   - `flexWrap: 'nowrap'` במובייל - שומר הכל בשורה אחת
   - `overflow: auto` - אפשר גלילה אם צריך

## טיפים

- אם העץ גדול מדי במובייל, לחץ − כמה פעמים
- אם רוצה לראות פרטים, לחץ + והתקרב לאזור המעניין
- כפתור ⊡ תמיד מחזיר למצב אופטימלי

## דוגמה לשימוש

```
סיטואציה: יש לי 4 קטגוריות עם 10 משימות כל אחת

במובייל (360px רוחב):
1. פותח מפת העץ → אוטומטית ב-50% zoom
2. רואה את כל 4 הקטגוריות במבט אחד
3. מחליק עם האצבע ימינה-שמאלה ומעלה-מטה → רואה את כל המפה
4. רוצה לראות משימה ספציפית → לוחץ + פעמיים (90% zoom)
5. מתקרב למשימה ורואה את הפרטים
6. גמרתי → לוחץ ⊡ לחזור למבט כללי
```

## תכונות מגע (Touch)

- **גלילה דו-כיוונית:** מחליק עם האצבע בכל כיוון (ימין, שמאל, מעלה, מטה)
- **WebKit smooth scrolling:** גלילה חלקה בדפדפני מובייל
- **Touch-friendly controls:** כפתורים גדולים שקל ללחוץ עליהם
- **No selection:** לא ניתן לסמן טקסט בטעות בזמן גלילה

## Fallback

אם transform scale לא עובד טוב במכשיר מסוים, אפשר תמיד לגלול ימינה-שמאלה (overflow-x: auto).
