# בדיקת שמירה אוטומטית 🧪

## איך לבדוק שהשמירה עובדת

### שלב 1: הפעל את השרת
```bash
npm run dev
```

### שלב 2: פתח את האתר
הדפדפן יפתח אוטומטית (בדרך כלל ב-`http://localhost:5173`)

### שלב 3: בדוק את האינדיקטור
בכותרת העליונה של האתר, ליד הלוגו, תראה:
- ☁️ "כל השינויים נשמרים אוטומטית"

### שלב 4: צור משימה חדשה
1. לחץ על הכפתור **"משימה חדשה"** (למעלה ליד חיפוש)
2. מלא את הפרטים:
   - כותרת: "בדיקת שמירה אוטומטית"
   - תיאור: "זו משימת בדיקה"
   - קטגוריה: בחר אחת מהקטגוריות הקיימות
   - אחראי: "בודק המערכת"
3. לחץ **"צור משימה"**
4. שים לב שהאינדיקטור משתנה ל-🔄 "שומר..." ואז ל-✅ "נשמר"

### שלב 5: ערוך משימה קיימת
1. לחץ על המשימה שיצרת (או כל משימה אחרת)
2. שנה את הכותרת ל-"בדיקה - עודכן"
3. לחץ **"שמור שינויים"**
4. שוב תראה "שומר..." ואז "נשמר"

### שלב 6: הוסף מיילסטון
1. באותה משימה, הוסף מיילסטון חדש בתיבת הטקסט למטה
2. לחץ על כפתור ה-Plus
3. המיילסטון נוסף ונשמר אוטומטית

### שלב 7: סמן מיילסטון כהושלם
1. לחץ על העיגול ליד המיילסטון
2. הוא יהפוך לסומן ✓ ונשמר אוטומטית
3. התקדמות המשימה תתעדכן

### שלב 8: **הבדיקה החשובה ביותר - רענון הדף!**
1. **רענן את הדף** (לחץ F5 או Ctrl+R)
2. המתן שהדף ייטען מחדש
3. **ודא שכל השינויים שעשית נשמרו**:
   - המשימה החדשה שיצרת קיימת
   - השינויים בכותרת נשמרו
   - המיילסטונים שהוספת קיימים
   - הסימונים של מיילסטונים מושלמים נשמרו

### שלב 9: בדוק ב-Developer Tools
1. פתח Developer Tools (F12)
2. עבור ללשונית **Application** (או **אחסון** בעברית)
3. בתפריט השמאלי, תחת **Local Storage**, בחר את הדומיין שלך
4. תראה את המפתחות:
   - `grow.tasks` - כל המשימות
   - `grow.milestones` - כל המיילסטונים
   - `grow.groups` - כל הקטגוריות
   - `grow.people` - אנשי קשר
   - `grow.taskWatchers` - עוקבים אחר משימות

## תרחישי בדיקה מתקדמים

### בדיקת מחיקה
1. צור משימה חדשה
2. פתח אותה
3. לחץ על כפתור הפח 🗑️ (ליד "שמור שינויים")
4. אשר את המחיקה
5. המשימה נמחקת ונשמרת המחיקה
6. רענן את הדף - המשימה לא תחזור

### בדיקת עריכה מרובה
1. ערוך 3 משימות שונות
2. הוסף מיילסטונים לכל אחת
3. סמן חלק מהמיילסטונים
4. רענן את הדף
5. כל השינויים צריכים להישמר

### בדיקת offline
1. נתק את האינטרנט (או סגור את ה-WiFi)
2. ערוך משימות והוסף מיילסטונים
3. רענן את הדף
4. כל השינויים צריכים להישמר (כי הם ב-localStorage)

## פתרון בעיות

### השינויים לא נשמרים?
1. בדוק שאין שגיאות ב-Console (F12)
2. ודא שה-localStorage לא מלא (זה נדיר מאוד)
3. נסה במצב Incognito - אולי יש הרחבה שחוסמת

### האינדיקטור לא מופיע?
1. ודא שאתה משתמש ב-`App.new-supabase.tsx` (זה קורה אוטומטית עכשיו)
2. בדוק ב-`src/main.tsx` שהשורה היא:
   ```typescript
   import App from './App.new-supabase.tsx'
   ```

### רוצה לאפס את כל הנתונים?
פתח Console והרץ:
```javascript
localStorage.clear();
location.reload();
```

## תכונות נוספות שנוספו

### יצירת משימות חדשות
- כפתור "משימה חדשה" בחלק העליון של תצוגת המשימות
- טופס פשוט עם כל השדות הנדרשים
- שמירה אוטומטית בלחיצה על "צור משימה"

### מחיקת משימות
- כפתור פח במגירת עריכת המשימה
- אישור לפני מחיקה
- מחיקה גם של כל המיילסטונים הקשורים

### ניהול קטגוריות
- יש עכשיו hooks מוכנים (useCreateGroup, useUpdateGroup, useDeleteGroup)
- ניתן להוסיף UI לניהול קטגוריות בעתיד

## ביצועים

- ⚡ **מהיר** - עדכונים אופטימיים (Optimistic Updates)
- 💪 **יעיל** - שמירה רק של מה שהשתנה
- 🎯 **אמין** - Rollback במקרה של שגיאה

## אבטחת מידע

- 🔒 localStorage מוגבל לדומיין הספציפי
- 🚫 אתרים אחרים לא יכולים לגשת לנתונים
- ⚠️ **הערה**: אם המשתמש ינקה את ה-localStorage של הדפדפן, הנתונים יאבדו
  - לשמירה קבועה יותר, יש להגדיר Supabase

## סיכום

המערכת עכשיו שומרת **אוטומטית** את כל השינויים:
- ✅ בעת יצירה
- ✅ בעת עריכה
- ✅ בעת מחיקה
- ✅ בעת עדכון מיילסטונים
- ✅ בעת הוספת אנשי קשר

**אין צורך לשמור ידנית** - הכל קורה באופן אוטומטי! 🎉
