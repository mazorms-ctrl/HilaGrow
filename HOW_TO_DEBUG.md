# איך לבדוק מה הבעיה

המסך ריק ב-http://localhost:5175 - בואו נבדוק מה קורה.

## שלב 1: פתח את Developer Tools

1. פתח את הדפדפן ב-`http://localhost:5175`
2. לחץ **F12** (או Right-Click → Inspect)
3. עבור ל-tab **Console**

## מה אתה רואה ב-Console?

### אם רואה שגיאות אדומות:
תעתיק את השגיאות ותשלח לי.

### אם לא רואה כלום:
עבור ל-tab **Network** ורענן את הדף (Ctrl+R או F5)

## שלב 2: בדוק Network

1. בתוך Developer Tools, לחץ על tab **Network**
2. רענן את הדף (F5)
3. האם רואה קבצים נטענים?
4. האם יש קבצים באדום (404/500)?

## שלב 3: בדוק Elements

1. עבור ל-tab **Elements**
2. האם יש תוכן ב-`<body>`?
3. האם יש `<div id="root">`?

## שלב 4: נסה Hard Refresh

1. לחץ **Ctrl + Shift + R** (Windows) או **Cmd + Shift + R** (Mac)
2. זה ימחק את ה-cache וירענן

## שלב 5: נסה דפדפן אחר

אם אתה ב-Chrome, נסה Firefox או Edge.

---

## תספר לי מה אתה רואה:

- [ ] שגיאות ב-Console (אילו?)
- [ ] קבצים לא נטענים ב-Network (אילו?)
- [ ] מסך ריק לגמרי
- [ ] רואה משהו (מה?)

זה יעזור לי לזהות את הבעיה!
